## Acceptance result

`REJECTED_FAIL_CLOSED`; the five-object runtime configuration was restored to its exact pre-change hashes.

Case A's independent Gate found that the supposed positive sealed-I/O path physically read a post-boundary synthetic sentinel before filtering, and the typed Gate-envelope preflight was not enforceable. Case B also lacked the required dependency fixture. No holdout bytes or candidate-conditioned performance were accessed; no duplicate successor was created.

This meets the approved rollback trigger: inability to prove fail-closed behavior. The OpenSpec record and replay evidence remain immutable. A future version must provide an executable, adapter-level pre-read-bound harness and typed envelope validator before another coordinated configuration transaction.
